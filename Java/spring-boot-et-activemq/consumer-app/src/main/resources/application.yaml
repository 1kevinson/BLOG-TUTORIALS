# CONSUMER SERVER PORT
server:
  port: 8082


# MESSAGE BROKER CONFIG
spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: adminpass
    packages:
      trust-all: true
  artemis:
    embedded:
      queues: messages
